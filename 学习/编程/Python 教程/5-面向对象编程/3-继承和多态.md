# 继承和多态

```python
class Animal(object):
    def run(self)
    	print('Animal is running...')
class Dog(Animal):
    def run(self):
        print('Eating meat...')
def run_twice(animal):
    animal.run()
    animal.run()
```

> ”开闭原则“
>
> 对扩展开放：允许新增子类
>
> 对修改封闭：不需要修改依赖类型的函数

#### 静态语言和动态语言
