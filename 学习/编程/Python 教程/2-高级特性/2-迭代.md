# 2- 迭代

```python
d={'a':1,'b':2,'c'}
for key in d:
    pass
for value in d.values():
    pass
for k,v in d.item()
```

```python
# 通过 collections 模块的 lterable 类型判断
from collections import lterable
isinstance('abc',lterable) # str 是否可迭代
```

```python
# Python 内置的 enumerate 函数可把 list 变为索引-元素对
for i,value in enumerate(['A','B'.'C']):
    print(i,value)
```
