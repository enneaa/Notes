# 测试主页

标签:: #记
日期::2023-03-28 19:15

> [!note] Have A Nice Day
>
> ```dataviewjs
> const str = "Hello，**神**！"
> dv.paragraph(str);
> let ftMd = dv.pages("").file.sort(t => t.cday)[0]
> let total = parseInt([new Date() - ftMd.ctime] / (60*60*24*1000))
> let totalDays = "你使用 **Obsidian** 已有 =="+total+"== 天。"
> let nofold = '!"misc/templates"'
> let allFile = dv.pages(nofold).file
> let totalMd = "期间一起共创建了 =="+
> 	dv.pages("").file.length+"== 篇文档"
> let totalTag = " =="+dv.pages("").file.tags.distinct().length+"== 个标签"
> let totalTask = " =="+allFile.tasks.length+"== 个待办。 <br><br>"
> dv.paragraph(
> 	totalDays
> )
> dv.paragraph(
> 	totalMd+"、"+totalTag+"、"+totalTask
> )
> ```
> ```dataviewjs
> // 修改其中的时间，可以输出当前离倒计时的时间差。
> const setTime = new Date("2024/01/01 00:00:00");
> const nowTime = new Date();
> const restSec = setTime.getTime() - nowTime.getTime();
> const day = parseInt(restSec / (60*60*24*1000));
> 
> const str = "距 **2024** 年还有" + day + "天"
> dv.paragraph(str);
> ```


> [!multi-column]
>
>> [!note] 最近新增
>> ```dataview
>> list from "" sort file.ctime desc limit 11
>> ``` 
>
>> [!example] 最近编辑
>> ```dataview
>> list from "" sort file.mtime desc limit 11
>> ```
